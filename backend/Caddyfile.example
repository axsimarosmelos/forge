# Replace with the hostname whose DNS points at the Forge API host.
compiler.example.com {
    request_body {
        max_size 64KB
    }
    reverse_proxy 127.0.0.1:8787 {
        transport http {
            response_header_timeout 15s
        }
    }
}
